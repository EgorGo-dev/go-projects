задача из Яндекс LMS - Яндекс Лицея |SQL|

Для таблицы products вычислите взвешенную среднюю длину названия, округлённую до 2-х знаков после запятой по цене:

weighted_avg_name_length = (∑(length(name)×price)) / ∑(price)
 

Формат вывода
Корректный вывод для тестовой сборки (тестирующая система преобразует вывод запроса в формат CSV):

weighted_avg_name_length
27.97